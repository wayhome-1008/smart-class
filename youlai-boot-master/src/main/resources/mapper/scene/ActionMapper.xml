<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youlai.boot.scene.mapper.ActionMapper">

    <!-- 获取执行器分页列表 -->
    <select id="getActionPage" resultType="com.youlai.boot.scene.model.vo.ActionVO">
        SELECT
                id,
                scene_id,
                type,
                device_ids,
                category_id,
                alert_rule_id,
                parameters
        FROM
            action
        <where>
        </where>
    </select>

</mapper>
